import { DiscoveryRpc } from "../requests";
export declare const wcRequestHandlerFactory: ({ rpc, network, onExecResult, authnBody, abortSignal, }: {
    rpc: DiscoveryRpc;
    network: string;
    onExecResult: (result: any) => void;
    authnBody: any;
    abortSignal: AbortSignal;
}) => ({}: {}) => Promise<{
    uri: string;
}>;
export declare function watchQrFactory({ rpc, authnBody, }: {
    rpc: DiscoveryRpc;
    authnBody: any;
}): ({ uri, approval, onExecResult, }: {
    uri: string;
    onExecResult: (result: any) => void;
    approval: any;
}) => void;
