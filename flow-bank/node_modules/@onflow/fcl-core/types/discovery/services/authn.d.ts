import { Service } from "@onflow/typedefs";
export declare const SERVICE_ACTOR_KEYS: {
    readonly AUTHN: "authn";
    readonly RESULTS: "results";
    readonly SNAPSHOT: "SNAPSHOT";
    readonly UPDATED: "UPDATED";
    readonly UPDATE_RESULTS: "UPDATE_RESULTS";
};
export interface ServiceData {
    results: Service[];
}
export type SubscriptionCallback = (data: Service[] | null, error: Error | null) => void;
export interface Authn {
    subscribe: (cb: SubscriptionCallback) => () => void;
    snapshot: () => Promise<ServiceData>;
    update: () => void;
}
declare const authn: Authn;
export default authn;
