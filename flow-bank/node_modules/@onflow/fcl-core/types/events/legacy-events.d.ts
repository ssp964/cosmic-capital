import type { Event } from "@onflow/typedefs";
import { FCLContext } from "../context";
export interface SubscribeObject {
    /**
     * @description Subscribe to events
     * @param callback The callback function
     * @returns A function to unsubscribe
     */
    subscribe: (callback: (data: Event | null, error: Error | null) => void) => () => void;
}
export declare function createLegacyEvents(context: Pick<FCLContext, "sdk" | "config">): (key: string) => SubscribeObject;
