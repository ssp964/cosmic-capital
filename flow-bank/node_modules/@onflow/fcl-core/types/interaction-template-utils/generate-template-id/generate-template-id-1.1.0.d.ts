import type { InteractionTemplate110 } from "../interaction-template";
import { FCLContext } from "../../context";
export declare function createGenerateTemplateId(context: Pick<FCLContext, "config" | "sdk">): ({ template, }: {
    template: InteractionTemplate110;
}) => Promise<string>;
export declare const generateTemplateId: ({ template, }: {
    template: InteractionTemplate110;
}) => Promise<string>;
