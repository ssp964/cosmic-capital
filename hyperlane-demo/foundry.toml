[profile.default]
src = "src"
out = "out"
libs = ["lib"]
solc_version = "0.8.26"
evm_version = "paris"

[rpc_endpoints]
origin = "${ORIGIN_RPC}"
hedera = "${HEDERA_RPC}"

[etherscan]
# optional, if you want to verify
