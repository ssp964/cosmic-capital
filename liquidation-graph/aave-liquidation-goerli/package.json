{
  "name": "aave-liquidation",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ aave-liquidation",
    "create-local": "graph create --node http://localhost:8020/ aave-liquidation",
    "remove-local": "graph remove --node http://localhost:8020/ aave-liquidation",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 aave-liquidation",
    "test": "graph test"
  },
  "dependencies": {
    "@aave/protocol-v2": "^1.0.1",
    "@graphprotocol/graph-cli": "0.97.1",
    "@graphprotocol/graph-ts": "0.37.0"
  },
  "devDependencies": {
    "matchstick-as": "0.6.0"
  }
}
